[pytest]
# Anchors pytest's rootdir to this directory, which makes the root conftest.py
# (and therefore the `from logic_utils import ...` in the tests) resolve even
# when pytest is invoked with an absolute path to tests/ from some other
# working directory. Without this, rootdir is inferred from the arguments and
# the root conftest.py is never collected.
testpaths = tests
